type BlogEntry {
    date: String!
    message: String!
    title: String!
}

type Blog {
    count: Int!
    nodes(offset: Int!, limit: Int!): [BlogEntry!]!
}

type Query {
    blog: Blog!
}

schema {
    query: Query
}
